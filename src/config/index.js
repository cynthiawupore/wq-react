/**
 *
 * 功能说明：    配置
 *
 * serverName           接口域名
 * apiCustomCode        接口常用code
 * apiPrefix            接口前缀
 * getToken             读token
 * setToken             写token
 *
 * */

import { serverName } from './server-name'
import { apiCustomCode } from './api-custom-code'
import { apiPrefix } from './api-prefix'

export {
    serverName,
    apiCustomCode,
    apiPrefix,
}
